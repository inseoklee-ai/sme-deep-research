# 과제 규정 — 나만의 딥리서치 에이전트

작업 전 반드시 읽는다. (2026-09-23 사용자가 전달한 과제 규정 정리)

## 진행 원칙 (사용자 지시)

- 단계마다 충분히 설명하고, **사용자가 이해·승인한 뒤에만** 다음 단계로 넘어간다.
- 아래 "직접 할 것" 3가지는 **진행 전에 사용자와 협의하고 승인을 받는다.**
- 저장소 공개(push)·배포 등 밖으로 나가는 일은 따로 확인받는다. API 키는 저장소에 올리지 않는다.

## 갖출 구조

```
질문
 ↓ ① 기획 — 목차를 짜고, 절마다 역할과 시작 자료를 배정
 ↓ ② 배치 — 서브에이전트 N명 동시 파견 (남의 구역은 알려 준다)
      각자 예산만큼 읽고 자기 절을 쓴다 (원문은 아래 남고, 위로는 원고만 올라간다)
 ↓ ③ 점검 — 부족하다고 신고한 절만 다시 내보낸다
 ↓ ④ 종합 — 이어 붙여 장문 보고서
 ↓ ⑤ 측정 — 정답표 없이 재고, 스위치를 하나씩 꺼서 무엇이 값을 했는지 가른다
```

## 직접 할 것 (코드는 에이전트에게, 이건 사람이)

1. **무엇을 절로 나눌 것인가** — 목차의 축이 곧 분업이고 결과물의 모양이다. 형식(개요·연표·분석)으로 나누면 전원이 같은 자료를 읽는다.
2. **대조군이 공정한가** — 혼자 하는 쪽에 같은 예산·모델·도구를 줬는지 직접 확인. 상대 손발을 묶고 이기면 실험이 아니다.
3. **에이전트 결과를 의심하기** — 흔한 사고: ① 지표가 좋아진 줄 알았는데 문장 분리 규칙이 바뀐 것 ② 스위치를 껐다고 로그만 찍고 코드 경로는 그대로 ③ 한 번 돌린 결과로 방향 단정(같은 설정도 시행마다 십몇 %p 움직인다).

## 필수 구현 5단계

1. **자료와 질문 세트**
   - 자료 30건 이상, 총 글자 수가 모델 창을 넘는 것을 숫자로 보인다. `links` 를 함께 만든다.
   - 질문 8건 이상, 성격이 갈리게: 한 건으로 답이 나오는 것 · 여러 갈래로 흩어진 것 · 한 대상을 따라가야 하는 것.
   - 정답표는 만들지 않는다. 질문마다 "이건 왜 나눌 만한가" 한 줄. 한 건으로 답이 나오는 질문을 일부러 넣는다.
2. **코디네이터와 서브에이전트**
   - 코디네이터가 목차(내용 단위) + 절마다 역할·시작 자료·예산 배정. 배정이 실제 자료를 가리키는지 코드가 검사.
   - 서브에이전트 동시 파견, 남의 구역 전달. 격리는 숫자로 — 코디네이터가 본 글자 수와 서브에이전트가 본 글자 수를 따로 센다.
   - 부족 자기신고 절만 재파견, 바퀴 상한. **두 번째 원고 처리 방침을 정하고 이유를 적는다** (무조건 덮어쓰면 인용 빠진 원고가 멀쩡한 원고를 지운다).
3. **무엇을 보고 판단할지**
   - 정답표·판정 모델 없이, 만들어진 글과 실제로 읽은 자료만 보는 지표.
   - 지표마다 어느 장치를 보는 신호인지 명시. 높낮이 신호와 0이어야 하는 경보를 가른다. 한 줄로 설명 안 되는 지표는 버린다.
4. **결과물을 직접 비교**
   - 같은 질문으로 기본 · 장치 하나 끈 것 · 혼자 하는 대조군. 대조군은 같은 자료·모델·도구·읽기 예산, 예산을 다 쓰는지 확인.
   - 나란히 읽고 어느 쪽이 나은지와 이유를 자기 말로. 지표는 명백한 실패 거르기용, 순위표 아님.
   - 마음에 안 드는 대목을 어느 절·어느 자료까지 추적 (서브에이전트 원고 vs 최종본).
   - 인용이 제자리에 붙었는지 원문과 대조 (읽은 문서를 엉뚱한 문장에 붙인 것은 사람만 잡는다).
5. **웹 데모** — streamlit/gradio. 보고서와 함께 절마다 누가 무엇을 읽고 무엇을 썼는지. 로컬 실행 필수, README 에 실행법 + 화면 캡처. 공개 배포는 선택.

## 제출물 (깃헙 주소)

```
data/            corpus.json (docs + links) · questions.json (질문과 "왜 나눌 만한가")
config.json      도메인에 묶인 값 (절수 · 절예산 · 바퀴 상한 · 역할 명단)
graph.py         기획 → 배치 → 서브에이전트 → 점검 → 종합
metrics.py       지표 — 정답표를 쓰지 않는다
ablation.py      스위치를 하나씩 끄고 재는 실험
baseline.py      혼자 하는 대조군 (예산을 맞춘다)
app.py           데모
output/          runs.jsonl · ablation.json · reports/
REPORT.md
```

### REPORT.md
주제와 코퍼스 · 분업 설계(서브에이전트 규칙, 역할 명단, 배정·구역 규칙) · 지표(무엇이 어느 장치의 신호인지) · 파이프라인 구조도(LangGraph 노드·State 흐름, Mermaid) · 데모 설계(캡처 포함) · 회고(가장 공들인 부분, 개선점)

### 루브릭
| 학습목표 | 평가기준 |
|---|---|
| 성능 검증 및 실패 분석 | 설정을 바꾼 보고서 여러 편을 나란히 읽고 어느 쪽이 나은지·이유를 자기 말로 적었나? 마음에 안 드는 대목을 어느 절·어느 자료까지 추적했나? |
| 분업 설계와 컨텍스트 격리의 타당성 | 목차를 내용 단위로 나누고 절마다 역할·시작 자료·예산을 타당하게 배정했나? |
| 엔드투엔드 구동 및 제출물 완결성 | 자료 수집부터 장문 보고서 생성까지 오류 없이 실행되나? |
